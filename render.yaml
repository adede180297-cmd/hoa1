services:
  - type: worker
    name: telegram-reminder-bot
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SHIFT_END
        value: "20:00"
